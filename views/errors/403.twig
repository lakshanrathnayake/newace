{% if user.role == 'ADMIN' %}
    {% extends 'admin.twig' %}
{% else %}
    {% extends 'manager.twig' %}
{% endif %}
{% block styles %}

{% endblock %}

{% block content %}
<div class="row" style="padding: 2%">
  
    <div class="col-md-12" style="text-align: center">
        <img src="/img/4032.png" style="display: inline" class="img img-responsive" />
        <h1>{{message}}</h1>
        <h2>{{error.status}}</h2>
        <p style="text-align: justify"><pre>Access Denied !!! Contact System Admin</pre></p>
        {#<a href="{{ url('authentication','logout') }}" class="btn btn-default">Login System</a>#}
    </div>
</div>
{% endblock %}

